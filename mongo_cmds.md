```javascript
db.createUser({
  user: "Sammy",
  pwd: "Rfin1ihe",
  roles: ["readWrite", "dbAdmin"]
});
```

- Set up a script that loaded the data into the comma database

db: comma
collection: trips

